To run the UpdateMe example app simply run the UpdateMe.exe application --> currently(October 2011) the version and update
files are stored in a Dropbox folder pointed to by the default values in the UpdateMe.exe application.

The version_updates_files folder contains an example update and an example version file.
Amend the URL in the um.txt file to point to where you have uploaded the update zip file.